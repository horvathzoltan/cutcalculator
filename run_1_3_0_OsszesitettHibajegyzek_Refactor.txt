Zoltán, rendben — **mostantól ebben a formátumban fogok patcheket adni**, pontosan a saját „ÚJ PATCH‑PROMPT (RUGALMAS STRICT MODE)” szabályaid szerint.

Ahhoz azonban, hogy **elkezdjük a PATCH SESSION‑t**, előbb **kell egyetlen, végleges, lineáris patch‑terv**, amelyet majd lépésről lépésre, snippetenként fogunk végrehajtani.

Ezért most **összeállítottam a végleges, egyetlen, lineáris, PATCH 1 → PATCH 7 tervet**, amelyet a következő üzenetben már a te formátumod szerint fogok snippetekre bontani.

---

# ⭐ **VÉGLEGES, LINEÁRIS PATCH‑TERV (PATCH 1 → PATCH 7)**
*(Ez az a terv, amelyet majd snippetenként fogunk végrehajtani a te formátumod szerint.)*

---

# **PATCH 1 — FileNameHelper bugfix + tisztítás**
**Cél:** A SnapshotManager UIState API‑ja hibás FileKind‑ra hivatkozik → javítjuk.

1.1 `FileKind` enum bővítése: `UIState_SnapshotFile`
1.2 `FILE_INFO` bővítése: `ui_snapshots/ui_state_%1.ini`
1.3 Régi `BOM_Workbench_SnapshotFile` törlése

---

# **PATCH 2 — SnapshotManager megtisztítása**
**Cél:** A régi WorkbenchSnapshot API teljes kivezetése.

2.1 Headerből törölni:
- `#include "workbench_snapshot.h"`
- `restoreSnapshot_BOMWorkbench()`
- `saveSnapshot_BOMWorkbench()`

2.2 CPP‑ből törölni a teljes implementációs blokkot.

---

# **PATCH 3 — workbench_snapshot.h törlése**
**Cél:** A régi snapshot formátum teljes kivezetése.

- A fájl teljes törlése
- A `.pro` fájlból is törölni

---

# **PATCH 4 — BOMWorkbench header tisztítása**
**Cél:** A régi snapshot API eltávolítása a Workbenchből.

- Törölni: `restoreState()`, `saveState()`

---

# **PATCH 5 — BOMWorkbench CPP tisztítása**
**Cél:** A régi percent‑based snapshot logika eltávolítása.

5.1 Törölni a teljes `restoreState()` metódust
5.2 Törölni a teljes `saveState()` metódust
5.3 Törölni a fölös include‑okat:
- `layout_default_store.h`
- `snapshot_manager.h`

---

# **PATCH 6 — Projekt tisztítása**
**Cél:** A régi snapshot rendszer maradványainak eltávolítása.

- `.pro` fájlból törölni: `workbench_snapshot.h`
- `.pro` fájlból törölni: `BOM_Workbench_SnapshotFile` enum használatát (ha van)

---

# **PATCH 7 — Ellenőrzés (nem kódmódosítás)**
**Cél:** Biztosítani, hogy az új UIStateCollector restore jó helyen fut.

- WindowStabilityMonitor → OK
- Workbench restore → `restoreUiState()`
- Mentés → `saveUiState()`

---

# ⭐ **Készen állunk a PATCH SESSION indítására**

Mostantól a folyamat így működik:

1. **Te megadod: „PATCH X kérem”**
2. Én:
   - ellenőrzöm, hogy minden szükséges fájl be van‑e küldve
   - ha hiányzik: bekérem
   - ha megvan: elkészítem a snippetet **pontosan a te formátumod szerint**
     - HELYMEGHATÁROZÁS
     - RÉGI → ÚJ diff
     - vagy „A megadott blokkot teljes egészében el kell távolítani.”

---

# ⭐ Indíthatjuk a PATCH SESSION‑t
Mondd ki:

👉 **„Kérem a PATCH 1‑et”**

és indul a snippet‑gyártás a te szabályaid szerint.




Zoltán, **PATCH SESSION elindítva** a te RUGALMAS STRICT MODE szabályaid szerint.
A PATCH 1‑hez **minden szükséges fájl megérkezett**, így indulhat a snippet‑gyártás.

---

# ⭐ **PATCH 1 — FileNameHelper bugfix + tisztítás**
**Cél:** A SnapshotManager UIState API‑ja hibás FileKind‑ra hivatkozik → hozzáadjuk a hiányzó enum értéket és a FILE_INFO bejegyzést, valamint eltávolítjuk a régi WorkbenchSnapshot fájlnevet.

---

# 🔧 **PATCH 1 / SNIPPET #1 — FileKind enum bővítése**
**Fájl:** `common/utils/filename_helper.h`
**Hely:** `enum class FileKind` blokk
**Cél:** Új enum érték hozzáadása: `UIState_SnapshotFile`

### RÉGI:
```cpp
    MainWindow_SnapshotFile,
    BOM_Workbench_SnapshotFile
```

### ÚJ:
```cpp
    MainWindow_SnapshotFile,
    BOM_Workbench_SnapshotFile,
    UIState_SnapshotFile
```

---

# 🔧 **PATCH 1 / SNIPPET #2 — FILE_INFO bővítése**
**Fájl:** `common/utils/filename_helper.cpp`
**Hely:** `static const QMap<FileKind, FileKindInfo> FILE_INFO = { ... }`
**Cél:** Új bejegyzés hozzáadása az UIState fájlhoz

### ÚJ BLOKK (nincs RÉGI, mert új bejegyzés):
```cpp
{ FileKind::UIState_SnapshotFile, { "ui_snapshots/ui_state_%1.ini", FileBehavior::Config } },
```

---

# 🔧 **PATCH 1 / SNIPPET #3 — Régi WorkbenchSnapshot fájlnevek törlése**
**Fájl:** `common/utils/filename_helper.cpp`
**Hely:** FILE_INFO táblázat
**Cél:** A régi WorkbenchSnapshot fájlnevet eltávolítjuk

### RÉGI:
```cpp
{ FileKind::BOM_Workbench_SnapshotFile, { "ui_snapshots/bom_workbench/%1.ini", FileBehavior::Config } }
```

### ÚJ:
*(A megadott blokkot teljes egészében el kell távolítani.)*

---

# ⭐ PATCH 1 KÉSZ
A FileNameHelper most már:

- **tudja kezelni az UIState fájlokat**,
- **nem tartalmazza a régi WorkbenchSnapshot fájlnevet**,
- **összhangban van a SnapshotManager új UIState API‑jával**.

---

# 👉 Következő lépés
Mondd ki:

**„Kérem a PATCH 2‑t”**

és folytatjuk a SnapshotManager tisztításával.