És itt jön a lényeg:

⭐ A p7 funkció (detail sorok GUI‑s szerkesztése) NEM HIÁNYZIK —

már majdnem teljesen kész van a projektben.

A rendszerben:

-   van detail‑view → CalculationModeDetailView
-   van detail‑presenter → CalculationModeDetailPresenter
-   van detail‑manager → CalculationModeDetailManager
-   van registry update → NeedCalculationDetailRegistry::updateFormula()
-   van formula editor dialog → FormulaEditorDialog
-   van material picker dialog → MaterialPickerDialog_2
-   van formula validátor →
    NeedCalculationDetailRegistry::isFormulaValid()

Ami nincs összekötve, és ezért nem működik a szerkesztés, az ez:

❌ A formula cella szerkesztése NEM hívja meg a registry update‑et

❌ A presenter NEM kapja meg a CalculationModesPresenter jelzését

❌ A detail‑view NEM jelzi vissza, hogy a user szerkesztett

❌ A manager csak a toolbar „✏️ Szerkesztés” gombjára reagál

Ezért a p7 tervet most újraalkotom, a valós kódbázis alapján.

------------------------------------------------------------------------

⭐ p7 – Végleges, valós kódbázisra szabott megvalósítási terv

„Detail sorok szerkesztése GUI‑ból”

Ez a terv lépésről lépésre végrehajtható, és pontosan a meglévő kódra
épül.

------------------------------------------------------------------------

⭐ p7.1 – A CalculationModesPresenter → CalculationModeDetailPresenter összekötése

Jelenleg:

-   A CalculationModesPresenter jelzi: modeActivatedForDetails(modeId)
-   A CalculationModeDetailPresenter viszont nem figyeli ezt a jelet

✔️ PATCH

A BOMWorkbench-ben vagy a MainWindow-ban, ahol a két presenter
példányosítva van:

    connect(modesPresenter, &CalculationModesPresenter::modeActivatedForDetails,
            detailPresenter, &CalculationModeDetailPresenter::onModeSelected);

Ez a hiányzó láncszem.

------------------------------------------------------------------------

⭐ p7.2 – A detail‑view formula celláinak szerkeszthetővé tétele

Ez már megvan:

    formItem->setFlags(formItem->flags() | Qt::ItemIsEditable);

Tehát a user tud szerkeszteni.

------------------------------------------------------------------------

⭐ p7.3 – A detail‑view jelezze, ha a user módosította a formulát

Ez hiányzik.

A CalculationModeDetailView NEM küld jelet arról, hogy a formula cella
megváltozott.

✔️ PATCH

A view‑ban:

    connect(_table, &QTableWidget::itemChanged, this, [this](QTableWidgetItem* item) {
        if (item->column() == 1) {
            QUuid detailId = _table->item(item->row(), 0)->data(Qt::UserRole).toUuid();
            emit request_edit_formula(detailId);
        }
    });

Ez a kulcs.

------------------------------------------------------------------------

⭐ p7.4 – A manager helyesen frissítse a registry‑t

A CalculationModeDetailManager már tartalmaz egy működő edit‑kezelőt:

    connect(_view, &CalculationModeDetailView::request_edit_formula,
            this, [this](const QUuid& detailId) {
                const auto* d = NeedCalculationDetailRegistry::instance().findById(detailId);
                auto updated = *d;
                updated.formula = item->text();
                NeedCalculationDetailRegistry::instance().update(updated);
    });

Ez jó, csak a view nem küldi a jelet.

------------------------------------------------------------------------

⭐ p7.5 – A registry menti a CSV‑t

Ez már működik:

    NeedCalculationDetailRegistry::updateWithWorkflow(updated);

→ workflow → persist → repository.save()

------------------------------------------------------------------------

⭐ p7.6 – A detail‑view újrarajzolása

A presenter már tudja:

-   registry változás → újratöltés
-   scroll a hibára

Ez is működik.

------------------------------------------------------------------------

⭐ p7.7 – Tesztelési lépések

1.  Indítsd el a programot
2.  Válassz egy terméket
3.  Válassz egy módot (pl. „mókus”)
4.  A detail‑táblázat megjelenik
5.  Kattints a formula cellára
6.  Írd át → Enter
7.  Ellenőrizd:
    -   registry frissült
    -   CSV mentődött
    -   GUI újrarajzolódott
    -   overlay frissült

------------------------------------------------------------------------

⭐ p7 – Összegzés

A detail‑szerkesztés működéséhez nem kell új view, nem kell új
presenter, nem kell új manager.

A rendszer már kész — csak három hiányzó drótot kell összekötni:

✔️ 1. CalculationModesPresenter → CalculationModeDetailPresenter

✔️ 2. DetailView itemChanged → request_edit_formula

✔️ 3. Manager update → registry.update → persist

Ha ez a három megvan → a detail szerkesztés tökéletesen működik.

------------------------------------------------------------------------

# CONTEXT SNAPSHOT — p7 (Detail sorok GUI-s szerkesztése)

## Jelenlegi állapot
- A NeedCalculationDetail CSV beolvasás és mentés teljesen működik.
- A detail sorok GUI-ban megjelennek (CalculationModeDetailView).
- A formula validátor működik (isFormulaValid).
- A detail sorok overlay státusza működik.
- A mókus mód zöld, a mátrix teljes.

## A probléma
A detail sorok GUI-ból való szerkesztése NEM működik, mert:
1. A CalculationModesPresenter NEM jelzi a CalculationModeDetailPresenter felé a mód kiválasztását.
2. A CalculationModeDetailView NEM küld jelet arról, hogy a user módosította a formula cellát.
3. A CalculationModeDetailManager csak a toolbar „✏️ Szerkesztés” gombjára reagál, cella-szerkesztésre nem.
4. A registry updateFormula() működik, de nincs meghívva cella-edit esetén.

## A megoldás (p7 patch)
1. Összekötni a CalculationModesPresenter → CalculationModeDetailPresenter jelet:
   connect(modesPresenter, &CalculationModesPresenter::modeActivatedForDetails,
           detailPresenter, &CalculationModeDetailPresenter::onModeSelected);

2. A CalculationModeDetailView-ben:
   - itemChanged jel → emit request_edit_formula(detailId)

3. A CalculationModeDetailManager-ben:
   - request_edit_formula(detailId) → registry.update(updatedDetail)

4. A registry workflow automatikusan menti a CSV-t.

5. A presenter registry változásra újratölti a detail sorokat.

## Eredmény
- A formula cella szerkesztése → azonnal mentődik CSV-be.
- A GUI frissül.
- A mátrix overlay frissül.
- A detail szerkesztés teljesen működik.

## Holnapra teendő
- p7.1: presenter összekötés implementálása
- p7.2: view itemChanged jel bekötése
- p7.3: manager update hívás ellenőrzése
- p7.4: manuális tesztelés (GUI)


***

# MODEL
  calculation/model/need_calculation_detail.h

  # REPOSITORY
  calculation/repository/need_calculation_detail_repository.h
  calculation/repository/need_calculation_detail_repository.cpp

  # REGISTRY
  calculation/registry/need_calculation_detail_registry.h
  calculation/registry/need_calculation_detail_registry.cpp

  # DETAIL VIEW
  calculation/view/calculation_mode_detail_view.h
  calculation/view/calculation_mode_detail_view.cpp

  # DETAIL PRESENTER
  calculation/presenter/calculation_mode_detail_presenter.h
  calculation/presenter/calculation_mode_detail_presenter.cpp

  # DETAIL MANAGER
  calculation/manager/calculation_mode_detail_manager.h
  calculation/manager/calculation_mode_detail_manager.cpp

  # MODES VIEW (bal panel)
  calcmodes/view/calculation_modes_view.h
  calcmodes/view/calculation_modes_view.cpp

  # MODES PRESENTER (bal panel)
  calcmodes/presenter/calculation_modes_presenter.h
  calcmodes/presenter/calculation_modes_presenter.cpp

  # MODES MANAGER
  calcmodes/manager/calculation_modes_manager.h
  calcmodes/manager/calculation_modes_manager.cpp

  # CALCULATION REGISTRY
  calcmodes/registry/need_calculation_registry.h
  calcmodes/registry/need_calculation_registry.cpp

  # MATERIAL LOOKUP
  materials/registry/material_registry.h
  materials/registry/material_registry.cpp

  # PRODUCT LOOKUP
  products/registry/product_registry.h
  products/registry/product_registry.cpp

  # FORMULA ENGINE + VALIDÁTOR
  calculation/service/formula_engine.h
  calculation/service/formula_engine.cpp
  calculation/registry/need_calculation_detail_registry.h
  calculation/registry/need_calculation_detail_registry.cpp

  # MATRIX VALIDÁTOR
  calculation/service/matrix_validator.h
  calculation/service/matrix_validator.cpp

  # DIALOGOK
  calculation/dialogs/formula_editor_dialog.h
  calculation/dialogs/formula_editor_dialog.cpp
  calculation/dialogs/material_picker_dialog2.h
  calculation/dialogs/material_picker_dialog2.cpp

  # WORKBENCH
  workbench/view/bom_workbench.h
  workbench/view/bom_workbench.cpp
