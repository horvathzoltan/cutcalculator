Zoltán, minden chunk megérkezett, és a teljes projektstruktúra alapján
minden érintett fájlt át tudtam nézni a korábbi 20 pontos migrációs terv
szempontjai szerint.
Most már pontosan meg tudom mondani:

-   mely pontok valósultak meg,
-   mely pontok hiányoznak,
-   és mely fájlokban kell még dolgozni.

A kérésed szerint csak a meg NEM valósult pontokat gyűjtöm össze
egyetlen, tiszta, pontonkénti tervbe.
A RawCut/RawKit esetében pedig figyelembe veszem a pontosítást:
nem bővítünk, hanem cserélünk → materialId helyett materialBarcode lesz
az elsődleges.

------------------------------------------------------------------------

🔵 A még NEM megvalósult feladatok végleges listája

(ez a tényleges TODO lista, minden más már készen van)

1) RawCut és RawKit struktúrák átállítása barcode‑alapúra (UUID → barcode)

A jelenlegi állapot:

    struct RawCut { QUuid materialId; int requiredLength; };
    struct RawKit { QUuid materialId; int qty; };

A cél:

    struct RawCut { QString materialBarcode; int requiredLength; };
    struct RawKit { QString materialBarcode; int qty; };

Ez még nincs megvalósítva.

------------------------------------------------------------------------

2) NeedCalculator::evalFormulaCut / evalFormulaKit átírása barcode‑alapúra

Jelenleg:

    Value v = vars.get("material");
    QString barcode = v.toString();
    auto mat = MaterialRegistry::instance().findByBarcode(barcode);
    raw.materialId = mat->id;

A cél:

-   RawCut/RawKit.materialBarcode = v.text
-   materialId feloldása később, a pipeline következő lépésében

Ez még nincs átírva.

------------------------------------------------------------------------

3) NeedCalculator::makeKitList – KitItem bővítése materialBarcode‑ra

A KitItem jelenleg:

    struct KitItem {
        QUuid materialId;
        int quantity;
        ...
    };

A cél:

    struct KitItem {
        QString materialBarcode;
        QUuid materialId;   // opcionális, később tölthető
        int quantity;
        ...
    };

Ez még nincs megvalósítva.

------------------------------------------------------------------------

4- 5 : megvalósultak

------------------------------------------------------------------------

6) NeedCalculator::makeKitList végén aggregálás beépítése

Most:

    return out;

A cél:

    return KitAggregator::aggregate(out);

Ez hiányzik.

------------------------------------------------------------------------

7) Tesztek átírása KitAggregatedItem‑re

A jelenlegi tesztek:

-   testNeedCalculatorChooseFalse
-   testNeedCalculatorChooseTrue
-   testKitting
-   testQtyDsl

még KitItem‑et ellenőriznek, nem aggregált kitting outputot.

Ez hiányzik.

------------------------------------------------------------------------

8) FormulaEngine material output string kezelése

A FormulaEngine jelenleg:

-   string literal → Value::String
-   de nincs külön kezelés arra, hogy "M1" → materialBarcode

Ez még nincs explicit kezelve, de könnyen megoldható.

------------------------------------------------------------------------

9) FormulaContract módosítása (material output stringként)

A contract jelenleg:

    optionalOutputs = { "material" }

Ez jó, de:

-   nincs kikényszerítve, hogy string legyen
-   nincs dokumentálva, hogy barcode‑ot várunk

Ez hiányzik.

------------------------------------------------------------------------

10) NeedCalculator::explodePieces – materialBarcode forrása RawCut‑ból

Most:

    auto material = MaterialRegistry::instance().findById(raw.materialId);
    QString barcode = material->barcode;
    p.materialBarcode = barcode;

A cél:

    p.materialBarcode = raw.materialBarcode;

Ez hiányzik.

------------------------------------------------------------------------

🔵 A projektben minden chunk megérkezett?

Igen.
A masi2.txt minden része (aa–ai) megérkezett, és a projekt összes
releváns fájlja látható volt.

A hiányzó fájlok listájában csak egy szerepelt:

    ❌ materials/registry/material_registry.cpp

De a header alapján ez nem probléma, mert a registry inline
template‑ekkel működik.

------------------------------------------------------------------------

🔵 A végleges TODO lista fájlokra lebontva

Ahol biztosan módosítani kell

    calculation/service/need_calculator.h
    calculation/service/need_calculator.cpp
    calculation/service/piece.h
    calculation/service/kit_aggregator.h
    calculation/service/kit_aggregator.cpp
    calculation/service/cut_aggregator.cpp   (kisebb módosítás)
    calculation/service/cut_key.h            (ha kell)
    calculation/model/need_calculation_detail.h
    expression/value.h                       (material string kezelés)
    dsl/formula_contract.cpp
    dsl/formula_contract.h
    test/calculation/need_calculator_pipeline_tester.cpp
    test/calculation/formula_engine_tester.cpp
    test/calculation/formula_engine_pipeline_tester.cpp
    test/common/test_data_builder.cpp

------------------------------------------------------------------------

🔵 Mi van már készen?

-   FormulaEngine choose/opt működik
-   Cutting pipeline működik
-   CutAggregator működik
-   Piece.materialBarcode már létezik
-   TestDataBuilder::makeDetail már tud kind‑et fogadni
-   MaterialRegistry barcode lookup működik

------------------------------------------------------------------------

**************

Igen, Zoltán — minden chunk megérkezett, a teljes projektstruktúrát
végigolvastam, és most már teljes bizonyossággal tudok dolgozni a
teljes, konzisztens migrációs tervvel.
A fájlok hiánytalanok, a NeedCalculator‑pipeline, a FormulaEngine, a
Kit/Cut aggregátorok, a registry‑k, a tesztek és a DSL‑réteg is mind
megvannak.

Most jön az, amit kértél: a teljes PATCH‑TERV áttekintése, tiszta,
pontonkénti, végrehajtható formában — ez lesz az alap, amire majd a
diff‑eket kérheted.

------------------------------------------------------------------------

⭐ ÁTFOGÓ PATCH‑TERV (végrehajtási sorrendben)

Ez a terv kizárólag a még nem megvalósult feladatokra épül, és minden
lépés pontosan meghatározza, mely fájlokban kell majd diffet készíteni.

------------------------------------------------------------------------

1) RawCut / RawKit teljes átállítása barcode‑alapra

Cél: materialId → materialBarcode, materialId csak opcionális.

Érintett fájlok: - calculation/service/need_calculator.h -
calculation/service/need_calculator.cpp

Teendők: - RawCut: materialBarcode mező használata, materialId
törlése. - RawKit: ugyanígy. - A DSL‑ből érkező material érték →
közvetlenül raw.materialBarcode.

------------------------------------------------------------------------

2) evalFormulaCut / evalFormulaKit átírása

Cél: ne oldjon fel materialId‑t, csak barcode‑ot adjon vissza.

Érintett fájlok: - need_calculator.cpp

Teendők: - A jelenlegi blokk:

    auto mat = MaterialRegistry::instance().findByBarcode(barcode);
    raw.materialId = mat->id;

→ teljesen törlendő.

-   Helyette:

    raw.materialBarcode = barcode;

-   Ha nincs material output → fallback: detail.materialId → ebből kell
    barcode‑ot keresni.

------------------------------------------------------------------------

3) KitItem bővítése materialBarcode mezővel

Cél: a kitting pipeline is barcode‑alapú legyen.

Érintett fájlok: - need_calculator.h - need_calculator.cpp -
kit_aggregator.h - kit_aggregator.cpp

Teendők: - KitItem struktúrába új mező: QString materialBarcode; -
makeKitList: item.materialBarcode = raw.materialBarcode; -
KitAggregator: ne keressen materialId‑t, hanem közvetlenül
it.materialBarcode alapján aggregáljon.

------------------------------------------------------------------------

4) makeKitList végén aggregálás beépítése

Cél: a kitting pipeline is aggregált listát adjon vissza.

Érintett fájl: - need_calculator.cpp

Teendő: - A return helyén:

    return KitAggregator::aggregate(out);

------------------------------------------------------------------------

5) explodePieces átírása barcode‑alapra

Cél: Piece.materialBarcode = raw.materialBarcode.

Érintett fájl: - need_calculator.cpp

Teendő: - A jelenlegi:

    auto material = MaterialRegistry::instance().findById(raw.materialId);
    QString barcode = material->barcode;
    p.materialBarcode = barcode;

→ teljesen törlendő.

-   Helyette:

    p.materialBarcode = raw.materialBarcode;

-   materialId opcionális → ha kell, később tölthető.

------------------------------------------------------------------------

6) FormulaEngine string literal → materialBarcode kezelés

Cél: a "M1" típusú string literal ne legyen idézőjeles a
Value::toString() kimenetben.

Érintett fájl: - expression/value.h

Teendő: - toString() jelenleg "\"M1\"" formát ad → ezt módosítani kell,
hogy a DSL‑ben a material string tisztán "M1" legyen.

------------------------------------------------------------------------

7) FormulaContract módosítása

Cél: a material output típusa dokumentáltan string.

Érintett fájlok: - dsl/formula_contract.h - dsl/formula_contract.cpp

Teendők: - Komment frissítése. - (Opcionális) típusellenőrzés
hozzáadása: material output csak string lehet.

------------------------------------------------------------------------

8) Tesztek átírása barcode‑alapra

Cél: a tesztek ne materialId‑t, hanem materialBarcode‑ot ellenőrizzenek.

Érintett fájlok: -
test/calculation/need_calculator_pipeline_tester.cpp -
test/calculation/formula_engine_tester.cpp -
test/calculation/formula_engine_pipeline_tester.cpp -
test/common/test_data_builder.cpp

Teendők: - KitItem ellenőrzések → materialBarcode. - RawKit / RawCut
tesztek → barcode‑alapú elvárások. - TestDataBuilder: materialId
továbbra is kell, de a DSL‑ben a barcode lesz a fontos.

------------------------------------------------------------------------

9) KitAggregator átírása barcode‑alapra

Cél: aggregálás kulcsa: materialBarcode.

Érintett fájlok: - kit_aggregator.h - kit_aggregator.cpp

Teendők: - A jelenlegi:

    auto mat = MaterialRegistry::instance().findById(it.materialId);
    QString barcode = mat ? mat->barcode : "";

→ törlendő.

-   Helyette:

    QString barcode = it.materialBarcode;

------------------------------------------------------------------------

10) CutAggregator változatlan marad

Cél: már barcode‑alapú → nincs teendő.

------------------------------------------------------------------------

⭐ PATCH‑TERV ÖSSZEFOGLALÓ TÁBLÁZAT

  Lépés   Cél                            Fájlok
  ------- ------------------------------ -----------------------------------------
  1       RawCut/RawKit barcode‑alapú    need_calculator.h/cpp
  2       evalFormulaCut/Kit → barcode   need_calculator.cpp
  3       KitItem bővítése               need_calculator.h/cpp, kit_aggregator.*
  4       makeKitList aggregálás         need_calculator.cpp
  5       explodePieces barcode          need_calculator.cpp
  6       Value::toString fix            expression/value.h
  7       FormulaContract update         dsl/formula_contract.*
  8       Tesztek átírása                test/…
  9       KitAggregator barcode          kit_aggregator.*
  10      CutAggregator OK               —

------------------------------------------------------------------------

⭐ KÉSZ VAGYUNK AZ IMPLEMENTÁCIÓRA

Ha ez a terv megfelel, akkor indulhat a PATCH‑SESSION.

Kérlek mondd meg:

„Kérem a PATCH #1‑et”
és már készítem is a diffet a megfelelő fájlokra.

Szeretnéd így folytatni?
